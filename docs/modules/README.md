# Deprecated: `docs/modules/`

This folder used to contain Design Module documents.

Design Modules are now consolidated under:

- `docs/design-modules/`

This directory is intentionally kept as a thin redirect to avoid breaking old links. Do not add new module docs here.
