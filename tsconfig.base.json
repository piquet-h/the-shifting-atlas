{
    "compilerOptions": {
        "target": "ESNext",
        "module": "ESNext",
        "moduleResolution": "node",
        "allowJs": false,
        "checkJs": false,
        "strict": true,
        "skipLibCheck": true,
        "esModuleInterop": true,
        "forceConsistentCasingInFileNames": true,
        "resolveJsonModule": true,
        "isolatedModules": true,
        "noEmitOnError": true,
        "types": ["node"],
        "baseUrl": "."
        /* Removed '@atlas/shared' path mapping to prevent source files from the shared package being
           pulled directly into dependent project compilation contexts (e.g. frontend) which caused
           TS6059/TS6307 rootDir and file list errors. Resolution now relies on workspace package
           semantics + project references so only built declaration outputs are consumed. */
    },
    "exclude": ["node_modules", "dist", "src/**/__tests__", "**/*.test.ts"]
}
