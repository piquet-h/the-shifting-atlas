# Local Development Environment Variables
# Copy this file to .env.development and fill in your local values
# NEVER commit .env.development with real credentials

# Persistence Mode
PERSISTENCE_MODE=memory
# PERSISTENCE_MODE=cosmos

# Cosmos DB Gremlin Configuration (when PERSISTENCE_MODE=cosmos)
# Auth uses Azure AD (Managed Identity / developer identity). Run `az login` locally.
COSMOS_GREMLIN_ENDPOINT=wss://localhost:8901/
COSMOS_GREMLIN_DATABASE=game
COSMOS_GREMLIN_GRAPH=world

# Cosmos DB SQL (Core) Configuration (AAD auth; no key required)
COSMOS_SQL_ENDPOINT=https://localhost:8081/
COSMOS_SQL_DATABASE=game

# Service Bus (optional for local dev)
# SERVICE_BUS_CONNECTION_STRING=your-connection-string-here

# AI/Model Provider API Keys (optional for local dev)
# MODEL_PROVIDER_API_KEY=your-api-key-here

# Application Signing Secret (optional for local dev)
# SIGNING_SECRET=your-signing-secret-here

# Key Vault Name (leave empty for local dev, managed identity in production)
# KEYVAULT_NAME=

# Application Insights (optional for local dev)
# APPLICATIONINSIGHTS_CONNECTION_STRING=your-connection-string-here

# Node Environment
NODE_ENV=development

# Strict mode (requires complete config or fails)
PERSISTENCE_STRICT=false
