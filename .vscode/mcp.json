{
    "inputs": [
        {
            "type": "promptString",
            "id": "atlas-mcp-access-token",
            "description": "Azure AD access token for Atlas MCP. IMPORTANT: paste ONLY the raw token. Obtain via: az account get-access-token --scope api://fecae6e9-696f-46e4-b1c8-5b471b499a24/shifting-atlas-api/.default --query accessToken -o tsv",
            "password": true
        }
    ],
    "servers": {
        "github-remote-mcp-server": {
            "url": "https://api.githubcopilot.com/mcp/",
            "type": "http"
        },
        "live-the-shifting-atlas": {
            "type": "sse",
            "url": "https://func-atlas.azurewebsites.net/runtime/webhooks/mcp",
            "headers": {
                "Authorization": "Bearer ${input:atlas-mcp-access-token}"
            }
        },
        "playwright": {
            "command": "npx",
            "args": ["@playwright/mcp@latest"],
            "type": "stdio"
        },
        "tailwindcss-mcp-server": {
            "command": "npx",
            "args": ["tailwindcss-mcp-server@latest"],
            "type": "stdio"
        }
    }
}
