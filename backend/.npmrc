# Scoped registry for @piquet-h packages (GitHub Packages)
@piquet-h:registry=https://npm.pkg.github.com

# Auth token is provided via environment variable at install time.
# Export GITHUB_PACKAGES_TOKEN before running npm install (local) or map CI secret.
//npm.pkg.github.com/:_authToken=${GITHUB_PACKAGES_TOKEN}
always-auth=true

# (Intentionally no raw token committed.)
