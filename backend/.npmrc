# Backend .npmrc - Local configuration for backend workspace
#
# This file declares the scoped registry for @piquet-h packages.
# Authentication is handled by actions/setup-node in CI via GITHUB_TOKEN.
# For local development, create ~/.npmrc with your PAT.

@piquet-h:registry=https://npm.pkg.github.com