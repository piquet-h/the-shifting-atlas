{
    "name": "the-shifting-atlas-backend",
    "version": "0.1.0",
    "private": true,
    "main": "dist/src/functions/*.js",
    "scripts": {
        "clean": "rm -rf dist .cache coverage",
        "build": "tsc",
        "watch": "tsc --watch --preserveWatchOutput",
        "typecheck": "tsc --noEmit",
        "test": "node --test --import=tsx test/*.test.ts",
        "predev": "npm run build",
        "dev": "concurrently -k -n tsc,host -c yellow,blue \"npm:watch\" \"func start\"",
        "start": "npm run build && func start",
        "package": "cp -r package.json package-lock.json node_modules dist && cd dist && zip -r ../dist.zip ."
    },
    "engines": {
        "node": "20.x",
        "npm": ">=10.0.0"
    },
    "dependencies": {
        "@azure/functions": "^4.8.0",
        "@azure/identity": "^4.13.0",
        "@piquet-h/shared": "^0.2.3",
        "applicationinsights": "^2.9.5",
        "ci": "^2.3.0",
        "zod": "^4.1.11"
    },
    "devDependencies": {
        "@types/node": "^24.4.0",
        "concurrently": "^9.2.1",
        "tsx": "^4.19.1"
    }
}
