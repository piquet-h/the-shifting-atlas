node_modules/
.env
.env.development
.env.local
.env.*.local
dist/
coverage/
*.tsbuildinfo
.DS_Store

# Package tarballs (build artifacts)
*.tgz

# TypeScript incremental build cache directories
**/.cache/

# Ignore any Functions local settings (front or backend) universally
**/local.settings.json
frontend/axe-report/
# Temporary scratch directory (general)
tmp/
*.backup.*
issue_details.json
issue_description.txt
analysis_result.json
rationale.txt

# (Removed) former ordering drift / resequence artifacts (filenames preserved if regenerated, still ignored)
drift-sample.json
resequence-delta.json
resequence-delta.md

# Generated ARM templates from Bicep build
infrastructure/*.json
!infrastructure/parameters.json

# Historical ordering artifacts directory (no longer produced)
artifacts/ordering/*.json
!artifacts/ordering/.gitkeep
.env.cosmos.production
