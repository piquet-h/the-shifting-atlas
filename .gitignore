node_modules/
.env
.env.development
.env.local
.env.*.local
dist/
coverage/
*.tsbuildinfo
.DS_Store

# TypeScript incremental build cache directories
**/.cache/

# Ignore any Functions local settings (front or backend) universally
**/local.settings.json
frontend/axe-report/
# Temporary files from implementation order automation
tmp/
*.backup.*
issue_details.json
issue_description.txt
analysis_result.json
rationale.txt

# Drift / resequence transient artifacts (defensive — ensure not committed if re-created)
drift-sample.json
resequence-delta.json
resequence-delta.md

# Generated ARM templates from Bicep build
infrastructure/*.json
!infrastructure/parameters.json

# Ordering artifacts (retain for metrics, but ignore in commits)
artifacts/ordering/*.json
!artifacts/ordering/.gitkeep
