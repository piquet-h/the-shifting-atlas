# Root .npmrc - Repository-wide npm configuration
# 
# Purpose:
#   1. Declare scoped registry for @piquet-h packages (GitHub Packages)
#   2. Enforce consistent engine & install behavior
#
# Authentication:
#   - CI/CD: actions/setup-node automatically configures auth via GITHUB_TOKEN
#   - Local dev: Create ~/.npmrc with: //npm.pkg.github.com/:_authToken=<your-PAT>
#     (requires PAT with read:packages scope)
#
# DO NOT commit authentication tokens to this file.

engine-strict=true
save-exact=false
legacy-peer-deps=false

# Scoped registry for @piquet-h packages
@piquet-h:registry=https://npm.pkg.github.com
