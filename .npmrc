## ---------------------------------------------------------------------------
## Root .npmrc (Committed)
## Purpose:
##   1. Declare scoped registry mappings for published internal packages.
##   2. Enforce consistent engine & install behavior locally.
##
## Important:
##   - Do NOT commit any real auth token here.
##   - GitHub Actions `actions/setup-node` (with `always-auth: true`) will inject
##     a user-level auth token automatically (NODE_AUTH_TOKEN env).
##   - Locally, to access private GitHub Packages you can either:
##       export NODE_AUTH_TOKEN=<a PAT with read:packages>
##     or create a user-level ~/.npmrc containing the auth line:
##       //npm.pkg.github.com/:_authToken=ghp_xxx
## ---------------------------------------------------------------------------
engine-strict=true
save-exact=false
legacy-peer-deps=false

## Active scope for shared packages:
@piquet-h:registry=https://npm.pkg.github.com

# Auth line intentionally NOT persisted; added dynamically in CI.
# (If you really need a local per-repo token, you may temporarily uncomment and
#  substitute a real token, but prefer user-level ~/.npmrc instead.)
# //npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
//npm.pkg.github.com/:_authToken=
